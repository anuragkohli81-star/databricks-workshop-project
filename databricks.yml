# This is a Databricks asset bundle definition for workshop_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: workshop_project
  uuid: 3bbff5e9-5048-4225-94a8-ee378c872272
 
include:
  - resources/*.yml
  - resources/*/*.yml
 
targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-0ebc481d-14f5.cloud.databricks.com
 
  prod:
    mode: production
    workspace:
      host: https://dbc-0ebc481d-14f5.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/benceeok@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/anuragkohli81@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: anuragkohli81@gmail.com
        level: CAN_MANAGE